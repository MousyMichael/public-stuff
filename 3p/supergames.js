window.onload = (e) => {
  var cache {
    url:window.location.href,
    host:window.location.hostname,
    sessionId:sessionId()
  };
  var allE = document.querySelectorAll("*");
  allE.forEach((e) => {
    
  });
}
